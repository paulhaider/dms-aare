import qs.Modules.Plugins

PluginSettings {
    pluginId: "aareGuru"

    StringSetting {
        settingKey: "city"
        label: "City"
        description: "aareguru city slug (e.g. bern, thun, biel)"
        defaultValue: "bern"
    }
}
